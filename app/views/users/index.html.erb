<ul><% @users.each do |user| %>
    <li><%= "IDナンバー#{user.id}"%>
    <%= link_to(user.name,"/users/#{user.id}") %>
    <%= "#{user.age}歳" %>
    <%= user.gender %>
    <%= "「#{user.description}」" %></li>
<% end %></ul>